{{- /*
  Files listed here are NOT applied to the target. This is how per-OS scripts
  are selected: only the matching OS's .chezmoiscripts/<os>/ directory survives.
  Add OS-specific dotfiles under the same blocks as the repo grows.
*/ -}}
{{ if ne .chezmoi.os "darwin" -}}
.chezmoiscripts/darwin/**
{{ end -}}
{{ if ne .chezmoi.os "linux" -}}
.chezmoiscripts/linux/**
{{ end -}}
